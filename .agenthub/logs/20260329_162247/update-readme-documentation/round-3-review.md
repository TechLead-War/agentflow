DECISION: keep
APPROVED: yes
SUMMARY: The README refresh now matches the current agentflow implementation closely enough to merge. It fixes the prior stale requirements/logging problems, updates the CLI examples and workflow description to the current code, and avoids reintroducing the inaccurate `notify` config claim.
CHECKS:
1. Does it run/build correctly? [pass] This is a README-only change, so it does not alter runtime paths. As a sanity check, `PYTHONPYCACHEPREFIX=/tmp/agentflow-pyc python3 -m compileall agentflow`, `python3 -m agentflow --help`, and `python3 -m agentflow config` all succeeded.
2. Does it solve the actual requested task? [pass] The diff now covers the README-vs-code mismatches the task called out. Installation/requirements align with `agentflow.config.check_api_keys()` accepting API keys or installed `claude`/`codex` CLIs; usage examples match `agentflow/cli.py` (`--prompt-file`, stdin via `-`, `status`, `clean`); feature and architecture text now matches the implemented repo analysis, complexity gate, optional research, structured 8-check review, pre-merge re-review, optional validation, and branch cleanup behavior in `repo_analysis.py`, `gate.py`, `researcher.py`, `worker.py`, `merger.py`, `validator.py`, and `state.py`.
3. Did it stay within scope and avoid regressions? [pass] The change stays within README scope and removes prior misinformation instead of adding new unsupported behavior. In particular, it no longer documents `notify` as a working runtime switch, which avoids the earlier regression against actual `cli.py` behavior.
4. Is the logic correct, including edge cases and failure cases? [pass] The updated wording reflects the important non-happy-path behavior in the code: only approved branches are merged, merged branches are the ones cleaned up when `cleanup_branches` is on, escalated branches are preserved, rebased branches are re-reviewed before merge, and validation can fail, trigger one automatic fix task, and roll back the base branch while saving merged work to a safe branch.
5. Is the code/design quality acceptable? [pass] The documentation is materially more consistent and precise. The install/requirements sections now agree with backend detection, the review section matches the structured reviewer contract in `prompts/templates.py` and `prompts/guardrails.py`, and the log example reflects current decision/check/summary-style review output instead of the legacy `LGTM` path.
6. Is the chosen approach justified versus alternatives? [pass] A targeted README update is the right fix for a docs drift task. The diff grounds the new claims in actual implementation details from the core modules instead of hand-waving or proposing code changes that are unnecessary for a documentation audit.
7. Did the target metric actually improve? [not_applicable] No metric or measurable objective was defined for this README update.
8. Should we keep, reject, or retry this change? [pass] Keep is the correct call. All review checks pass, and the specific round-2 failures were addressed: CLI-only backends are now documented in Requirements/Install, the Logs section no longer describes the legacy `LGTM` flow, and the inaccurate `notify` config claim is gone.